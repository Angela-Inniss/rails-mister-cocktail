<h1>Add a shot of something</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient, collection:Ingredient.all %>
  <%= f.input :description %>
  <%= f.submit "Add dose", class: "btn btn-primary" %>
<% end %>
